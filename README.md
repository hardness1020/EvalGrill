# EvalGrill
Design trustworthy evaluations for AI tasks

Ships as a Claude Code plugin under [`plugin/`](plugin/README.md) — see that README for the dev-install loop (`claude --plugin-dir ./plugin`).
